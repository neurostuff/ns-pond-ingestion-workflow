"""
Step 1. Download Articles.
This module will download the articles
using the specified order of extractors,
(set in the settings) by
checking the cache for downloaded results
(specific to each extractor) and only downloading
the uncached files, updating the cache and
returning all downloaded articles (cached and new).
"""
