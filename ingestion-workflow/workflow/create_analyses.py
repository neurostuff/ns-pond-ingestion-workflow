"""
Step 3. Create analyses for extracted tables.
This module will check the cache for created_analyses,
depending on settings, and then run create_analyses
on the uncached files, updating the cache and
returning all created analyses (cached and new).
"""
